/* This is an attempt to solve this algorithm before the example was provided 

 -Write a function called "Same" which accepts two arrays
 - the fucntion should return true if every value in the array has it's correspoding value - 
 squared in the second array. the frequency of values must be the same. */
 
let arr = [1, 2, 3]
let arr2 = [4, 1, 9]

function same(arr, arr2) {
    // take the two arrays
    // compare values in order?
    // check for values even if it is not in order

    return true
    // return false
 }

same(arr, arr2)

